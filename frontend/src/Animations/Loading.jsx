import React from "react";

const Loading = () => {
    return (
        <svg className="mr-5 size-5 border-2 border-(--border) border-t-(--primary) rounded-full animate-spin"></svg>
    );
};

export default Loading;
